// Name: Reverse Text
// Description: Simple text reversal
// Avatar: ↩️

function encode(text) {
    return [...text].reverse().join('');
}

function decode(text) {
    return [...text].reverse().join('');
}
